# Mad-Island-Uncensor
Script created to remove the mosaic from Mad Island. 
It looks for a shader with the text "MosaicField" in the file data.unity3d.
Then it sets all colMask "val = 15" to "val = 0".
